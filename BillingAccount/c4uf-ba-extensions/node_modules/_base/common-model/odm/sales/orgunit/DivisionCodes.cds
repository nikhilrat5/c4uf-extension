namespace sap.odm.sales.orgunit;

using {sap.odm.common.codeList} from '../../common';

//TODO: Check if a separate entity needs to be created
/**
 * A way of grouping materials, products, or services.
 */
@odm.doc.SAPDelivered : false
@odm.doc.extensible   : true
entity DivisionCodes : codeList {
      /**
       * Code list entry.
       */
  key code : String(2);
}

/**
 * A specific division.
 */
type DivisionCode : Association to one DivisionCodes;
