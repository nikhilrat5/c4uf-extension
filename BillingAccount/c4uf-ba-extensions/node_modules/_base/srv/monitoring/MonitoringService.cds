using { HealthReponse } from './Types';

service MonitoringService @(path: '/monitoring', protocol: 'rest') {

    /*
     * Health check endpoint with UP/DOWN statuses
     */ 
    function health() returns HealthReponse;
}