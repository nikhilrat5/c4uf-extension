namespace com.sap.mdm.bp;

context EventMessagingModel {
  entity EventMessage {
    key MessageNumber   : Integer64;
        ObjectType      : String(5);
        ObjectNumber    : UUID;
        Status          : String(2);
        LastChangedAt   : Timestamp;
        ChangedEntities : String(5000);
  }
}
